name: "Example simple"

on:
  push:

jobs:
  print-hello:
    name: "Print Hello"
    runs-on: ubuntu-latest
    steps:
      - name: "Print Hello"
        shell: bash
        run: echo "Hello, world!"
